
version: '2.1'

services:
  PODServices:
    network_mode: bridge

    image: ecare
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: ecare
    ports:
      - 8081:8081
    expose:
            - 8081
    depends_on:
      - db
    links:
      - db
environment:

spring.datasource.url=jdbc:mysql://ec2-3-17-188-70.us-east-2.compute.amazonaws.com/userdetails
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.hibernet.dialect=org.hibernet.dialect.MySQL5Dialect




 
service:
  db:
   network_mode: bridge
    image: mysql:5.5.62
    container_name: db
    


    ports:
      - "6033:3306"
    volumes:
      - dbdata:/var/lib/mysql
  phpmyadmin:
    image: mysqlimage/mysqlimage
    container_name: pma
    links:
      - db
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      PMA_ARBITRARY: 1
    restart: always
    ports:
      - 8081:80
volumes:
  dbdata:
